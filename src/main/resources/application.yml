spring:
  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/bankdb
    password: Zaira885.
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    show_sql: true
    hibernate:
      ddl-auto: create
server:
  port: '5050'
